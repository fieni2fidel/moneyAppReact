import React from 'react';
import {style} from './index.scss';

const Counter = ({}) => {

    return (
        <div className={style}>
            
        </div>
    );
};

Counter.propTypes = {

};

export default Counter;
